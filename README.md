""" Revert Sheet Review Tool — Streamlit ===================================== Install: pip install streamlit pandas openpyxl Run: streamlit run revert_sheet_tool.py Workflow -------- 1. Upload Excel / CSV revert sheet 2. Coder → sees own FAIL items → marks Agree / Disagree + optional note 3. Auditor → sees Disagree items where they raised the fail (read-only, pending SME) 4. SME → reviews all Disagree items → closes each with a final note 5. Export → downloads the updated sheet as Excel with all responses filled in """import streamlit as st import pandas as pd import io from datetime import datetime # ── Page config ─────────────────────────────────────────────────────────────── st.set_page_config( page_title="Revert Sheet Review Tool", page_icon="📋", layout="wide", initial_sidebar_state="expanded", ) # ── Custom CSS ──────────────────────────────────────────────────────────────── st.markdown(""" <style> html, body, [class*="css"] { font-family: 'DM Mono', 'Courier New', monospace; } .main { background: #0f1117; } section[data-testid="stSidebar"] { background: #1a1d27 !important; } section[data-testid="stSidebar"] * { color: #e2e8f0 !important; } div[data-testid="metric-container"] { background: #1e2235; border: 1px solid #2e3350; border-radius: 10px; padding: 10px 16px !important; } div[data-testid="metric-container"] label { color: #64748b !important; font-size: 11px !important; } div[data-testid="metric-container"] div[data-testid="stMetricValue"] { font-size: 22px !important; font-weight: 800 !important; color: #e2e8f0 !important; } .rsr-card { background:#22263a; border:1px solid #2e3350; border-radius:12px; padding:18px 20px; margin-bottom:14px; } .rsr-card-amber { border-left:5px solid #f59e0b; } .rsr-card-red { border-left:5px solid #f43f5e; } .rsr-card-purple { border-left:5px solid #a855f7; } .rsr-card-green { border-left:5px solid #22c55e; } .rsr-card-muted { border-left:5px solid #64748b; } .badge { display:inline-block; padding:2px 11px; border-radius:20px; font-size:11px; font-weight:700; letter-spacing:.5px; margin-right:4px; } .badge-pass { background:#0f2d1e; color:#22c55e; border:1px solid #22c55e44; } .badge-fail { background:#2d0f18; color:#f43f5e; border:1px solid #f43f5e44; } .badge-agree { background:#0f2d1e; color:#22c55e; border:1px solid #22c55e44; } .badge-disagree { background:#2d0f18; color:#f43f5e; border:1px solid #f43f5e44; } .badge-closed { background:#1a1d27; color:#64748b; border:1px solid #64748b44; } .info-amber { background:#2d1f0a; border:1px solid #f59e0b44; border-radius:8px; padding:10px 14px; margin:8px 0; } .info-red { background:#2d0f18; border:1px solid #f43f5e44; border-radius:8px; padding:10px 14px; margin:8px 0; } .info-purple{ background:#1f0f2d; border:1px solid #a855f744; border-radius:8px; padding:10px 14px; margin:8px 0; } .card-id { font-size:15px; font-weight:700; color:#e2e8f0; } .card-desc { font-size:12px; color:#64748b; margin-top:3px; } .meta-label{ font-size:10px; font-weight:700; color:#64748b; letter-spacing:.5px; } .meta-val { font-size:12px; color:#e2e8f0; margin-top:1px; } .section-hd{ font-size:11px; font-weight:700; color:#64748b; letter-spacing:.8px; margin-bottom:6px; } hr { border-color:#2e3350 !important; margin:6px 0 !important; } .stTextArea textarea { background:#1e2235 !important; color:#e2e8f0 !important; border:1px solid #2e3350 !important; border-radius:8px !important; } .stButton button { border-radius:8px !important; font-weight:600 !important; font-size:13px !important; padding:6px 18px !important; } div[data-testid="stDownloadButton"] button { background:#0f2d1e !important; color:#22c55e !important; border:1px solid #22c55e55 !important; } </style> """, unsafe_allow_html=True) # ── Session state bootstrap ───────────────────────────────────────────────────for key, default in { "df": None, "file_name": "", "role": "Coder", "active_name": "", "view": "Queue", }.items(): if key not in st.session_state: st.session_state[key] = default # ── Helpers ───────────────────────────────────────────────────────────────────def find_col(columns, patterns): cleaned = {c: c.lower().replace(" ", "").replace("_", "") for c in columns} for p in patterns: p2 = p.lower().replace(" ", "").replace("_", "") for orig, low in cleaned.items(): if p2 in low: return orig return Nonedef normalize_df(raw: pd.DataFrame) -> pd.DataFrame: cols = list(raw.columns) id_c = find_col(cols, ["id","itemid","caseid","ticketid","no","sl"]) cod_c = find_col(cols, ["codername","coder","agent","employee"]) aud_c = find_col(cols, ["auditorname","auditor","reviewer","qa"]) sta_c = find_col(cols, ["finalstatus","status","result","outcome"]) des_c = find_col(cols, ["description","item","case","query","details","remark","type"]) com_c = find_col(cols, ["auditorcomment","comment","remarks","notes","reason","feedback"]) df = pd.DataFrame() df["ID"] = raw[id_c].astype(str) if id_c else [f"ITEM-{i+1}" for i in range(len(raw))] df["Coder Name"] = raw[cod_c].astype(str).str.strip() if cod_c else "Unknown" df["Auditor Name"] = raw[aud_c].astype(str).str.strip() if aud_c else "Unknown" df["Final Status"] = raw[sta_c].astype(str).str.lower().str.strip() if sta_c else "fail" df["Description"] = raw[des_c].astype(str) if des_c else "" df["Auditor Comment"] = raw[com_c].astype(str) if com_c else "" df["Coder Response"] = "" df["Coder Note"] = "" df["SME Comment"] = "" df["SME Closed"] = "" df["Last Updated"] = ""return df.reset_index(drop=True) def clean(val): v = str(val).strip() return "" if v.lower() in ("", "nan", "none") else v def status_badges(row): s = str(row.get("Final Status", "")).lower() b = f'<span class="badge badge-{"pass" if s=="pass" else "fail"}">{s.upper()}</span>' resp = str(row.get("Coder Response", "")).lower() if resp in ("agree", "disagree"): b += f'<span class="badge badge-{resp}">{resp.upper()}</span>'if str(row.get("SME Closed", "")).lower() == "yes": b += '<span class="badge badge-closed">CLOSED</span>'return b def to_excel_bytes(df: pd.DataFrame) -> bytes: from openpyxl import Workbook from openpyxl.styles import Font, PatternFill, Alignment from openpyxl.utils import get_column_letter wb = Workbook() ws = wb.active ws.title = "Review Data" headers = list(df.columns) wf_cols = {"Coder Response","Coder Note","SME Comment","SME Closed","Last Updated"} cmap = {"pass":("0f2d1e","22c55e"),"fail":("2d0f18","f43f5e"), "agree":("0f2d1e","22c55e"),"disagree":("2d0f18","f43f5e"),"yes":("1f0f2d","a855f7")} for ci, h in enumerate(headers, 1): cell = ws.cell(row=1, column=ci, value=h) cell.font = Font(bold=True, color="E2E8F0", name="Arial", size=10) cell.fill = PatternFill("solid", fgColor="1e2d5a" if h in wf_cols else "0f1117") cell.alignment = Alignment(horizontal="center", vertical="center", wrap_text=True) ws.column_dimensions[get_column_letter(ci)].width = max(16, len(h)+4) ws.row_dimensions[1].height = 28for ri, row in df.iterrows(): for ci, h in enumerate(headers, 1): val = str(row[h]) if pd.notna(row[h]) else "" cell = ws.cell(row=ri+2, column=ci, value=val) cell.font = Font(name="Arial", size=10, color="E2E8F0") cell.alignment = Alignment(vertical="center", wrap_text=True) vl = val.lower() if vl in cmap: cell.fill = PatternFill("solid", fgColor=cmap[vl][0]) else: cell.fill = PatternFill("solid", fgColor="1a1d27" if ri%2==0 else "22263a") ws.row_dimensions[ri+2].height = 20 ws.freeze_panes = "A2" buf = io.BytesIO() wb.save(buf) return buf.getvalue() # ═══════════════════════════════════════════════════════════════════════════════# SIDEBAR# ═══════════════════════════════════════════════════════════════════════════════with st.sidebar: st.markdown("## 📋 RSR Tool") st.markdown("---") uploaded = st.file_uploader("Upload Revert Sheet (.xlsx / .csv)", type=["xlsx","xls","csv"]) if uploaded: try: raw = (pd.read_csv(uploaded, dtype=str) if uploaded.name.endswith(".csv") else pd.read_excel(uploaded, dtype=str)) st.session_state.df = normalize_df(raw.fillna("")) st.session_state.file_name = uploaded.name st.session_state.active_name = "" st.success(f"Loaded: {uploaded.name}") except Exception as e: st.error(f"Error: {e}") st.markdown("---") st.markdown("**ROLE**") role = st.radio("role", ["Coder","Auditor","SME"], index=["Coder","Auditor","SME"].index(st.session_state.role), label_visibility="collapsed") if role != st.session_state.role: st.session_state.role = role st.session_state.active_name = "" st.session_state.view = "Queue" st.rerun() df = st.session_state.df if df is not None: st.markdown("---") st.markdown("**SELECT NAME**") if role == "Coder": names = sorted(df["Coder Name"].dropna().unique().tolist()) elif role == "Auditor": names = sorted(df["Auditor Name"].dropna().unique().tolist()) else: names = ["SME Panel"] chosen = st.selectbox("Name", names, index=names.index(st.session_state.active_name) if st.session_state.active_name in names else 0, label_visibility="collapsed") if chosen != st.session_state.active_name: st.session_state.active_name = chosen st.session_state.view = "Queue" st.rerun() st.markdown("---") st.markdown("**VIEW**") view_sel = st.radio("view", ["Queue","History"], index=["Queue","History"].index(st.session_state.view), label_visibility="collapsed") if view_sel != st.session_state.view: st.session_state.view = view_sel st.rerun() st.markdown("---") st.download_button( label="⬇ Export Updated Excel", data=to_excel_bytes(df), file_name=f"revert_sheet_{datetime.now().strftime('%Y%m%d_%H%M')}.xlsx", mime="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", use_container_width=True, ) # ═══════════════════════════════════════════════════════════════════════════════# MAIN# ═══════════════════════════════════════════════════════════════════════════════ df = st.session_state.df if df is None: st.markdown("## Revert Sheet Review Tool") st.info("Upload your Excel or CSV revert sheet from the sidebar to get started.") st.markdown(""" **Expected columns** (names are detected automatically): | Field | Accepted column names | |---|---| | Coder Name | codername, coder, agent | | Auditor Name | auditorname, auditor, reviewer | | Final Status | finalstatus, status, result | | Description | description, item, case, query | | Auditor Comment | auditorcomment, comment, remarks | """) st.stop() # Stats c1,c2,c3,c4,c5,c6,c7 = st.columns(7) c1.metric("Total", len(df)) c2.metric("✅ Pass", int((df["Final Status"]=="pass").sum())) c3.metric("❌ Fail", int((df["Final Status"]=="fail").sum())) c4.metric("⏳ Pending",int(((df["Final Status"]=="fail")&(df["Coder Response"]=="")).sum())) c5.metric("👍 Agree", int((df["Coder Response"]=="agree").sum())) c6.metric("👎 Disagree",int((df["Coder Response"]=="disagree").sum())) c7.metric("🔒 Closed", int((df["SME Closed"]=="Yes").sum())) st.markdown("---") role = st.session_state.role name = st.session_state.active_name view = st.session_state.view if not name: st.info("Select your name from the sidebar to continue.") st.stop() # Filterif role == "Coder": q_mask = (df["Final Status"]=="fail") & (df["Coder Name"]==name) & (df["Coder Response"]=="") h_mask = (df["Coder Name"]==name) & ((df["Final Status"]=="pass") | (df["Coder Response"]!="")) elif role == "Auditor": q_mask = (df["Final Status"]=="fail") & (df["Auditor Name"]==name) & \ (df["Coder Respo
 
